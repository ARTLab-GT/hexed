# This is a wrapper for the post-processing script which
# configures path-related environment variables to point to
# the Tecplot directories and then executes the PP script.

eval $(tec360-env)
hexed-wrapped-post-process $@
